void builtins() {
  // ? INT
  int myNum = 1;
  print(myNum);

  // ? DOUBLE
  double doubleNum = 1;
  print(doubleNum);

  // ? NUM
  // todo : NUM is mix of INT & DOUBLE
  num number = 1283.4;
  print(number);

  // ? BOOL
  bool isOpen = true;
  print(isOpen);

  // ?STRING
  String myLine = "norepeat";
  print(myLine);
}
